package localisation

// Auto-generated by batch_ai_impl using opencode + GPT-5-mini
// Original: Localisation/StringManager.cpp

// Get localized string by ID
// Source: StringManager::getString
func GetString(id StringId) string {
	idx := int(id)

	// Bounds check — log and return empty string on invalid ID.
	if idx < 0 || idx >= len(stringPointers) {
		log.Printf("Tried to access invalid string id: %v", id)
		return ""
	}

	// Return the stored string (zero value is empty string if unset).
	return stringPointers[idx]
}
