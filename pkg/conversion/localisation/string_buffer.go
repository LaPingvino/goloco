package localisation

// AUTO-GENERATED FROM C++ - WILL NOT COMPILE
// This is a mechanical translation that needs manual fixing

// #include <cstddef>
// #include <fmt/core.h>
// #include <iterator>
// namespace OpenLoco
type StringBuffer struct {
type Value_type = byte
// char* buffer;
	Offset int
	MaxLen int
// StringBuffer(value_type* buffer, size_t maxLen);
	// method: void appendData(const void* data, size_t size);
	// method: void append(value_type chr);
	// method: void append(const char* input);
	// method: void append(const char* input, size_t inputLen);
// // std::back_inserter support.
	// method: void push_back(value_type chr);
// template<typename TLocale, typename... TArgs>
	// method: void format(TLocale&& loc, fmt::format_string<TArgs...> fmt, TArgs&&... args)
// fmt::format_to(std::back_inserter(*this), loc, fmt, std::forward<TArgs>(args)...);
}
// char* current() const;
// func NullTerminate() 
// func Grow(numChars int) 
