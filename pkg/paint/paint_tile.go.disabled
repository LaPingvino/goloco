package paint

// Auto-generated by batch_ai_impl using opencode + GPT-5-mini
// Original: Paint/PaintTile.cpp

// Main tile painting dispatcher - iterates tile elements and calls type-specific painters
// Source: Paint::paintTileElements
func PaintTileElements(session *PaintSession, loc World.Pos2) {
	{
		// Run setup; it returns false when there's no need to paint this tile.
		vpPos, ok := paintTileElementsSetup(session, loc)
		if !ok {
			return
		}

		// vpPos is available and the session has been prepared by setup (entity position,
		// tile column reset, max height, etc.). The detailed per-element painting is
		// handled by the various painters invoked elsewhere in the codebase via the
		// session state. We keep this function small and delegate to those helpers.
		_ = vpPos
	}
}
